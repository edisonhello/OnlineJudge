#include <bits/stdc++.h>
using namespace std;

const int maxn = 100000 + 5;
int n, q, a[maxn];
vector<int> G[maxn];

int main() {
    ios_base::sync_with_stdio(false); cin.tie(0);
    cin >> n >> q;
    for (int i = 1; i <= n; ++i) cin >> a[i];
    return 0;
}
