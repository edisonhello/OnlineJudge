#include <iostream>
#include <map>
using namespace std;

int main() {
  map<char, bool> type;
  map<char, bool> visit;
  int n;
  cin >> n;
  // char c;
  char flat[100000];
  for (int i = 0; i < n; ++i) {
    cin >> flat[i];
    if (num.find(flat[i]) == num.end()) {
      num[flat[i]] = true;
    }
    num[flat[i]] = true;
  }
  for (int i = 0; i < n; ++i) {
    if (visit.size() == type.size()) {
      break;
    }
    if (visit.find(flat[i]) == )
  }
}
