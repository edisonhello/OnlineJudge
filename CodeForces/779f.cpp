#include <bits/stdc++.h>
using namespace std;

const int maxn = 300000 + 5;
int n;
vector<int> G[maxn];

int main() {
  ios_base::sync_with_stdio(false); cin.tie(0);
  cin >> n;
  for (int i = 0; i < n - 1; ++i) {
    int a, b;
    char c;
    cin >> a >> b >> c;
  }
}
