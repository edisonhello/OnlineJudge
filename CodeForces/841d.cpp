#include <bits/stdc++.h>
using namespace std;

const int maxn = 3e5 + 10;
int d[maxn];

int main() {
    ios_base::sync_with_stdio(false); cin.tie(0);
    int n, m; cin >> n >> m;
    for (int i = 1; i <= n; ++i) cin >> d[i];
    return 0;
}
