#include <bits/stdc++.h>
using namespace std;

typedef long long ll;

int main() {
    ios_base::sync_with_stdio(false); cin.tie(0);
    srand(time(nullptr));
    freopen("in.txt", "w", stdout);
    for (int i = 0; i < 5000; ++i) cout << 'a'; cout << endl;
    cout << 1000000 << endl;
    for (int i = 0; i < 1000000; ++i) {
        cout << 1 << ' ' << 1000000 << endl;
    }
}
