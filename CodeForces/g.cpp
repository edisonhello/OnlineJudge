#include <bits/stdc++.h>
using namespace std;

typedef long long ll;

set<pair<int, int>> s;

int main() {
    ios_base::sync_with_stdio(false); cin.tie(0);
    srand(time(nullptr));
    freopen("in.txt", "w", stdout);
    cout << 1000000 << endl;
    for (int i = 0; i < 1000000; ++i) cout << 5000000 << ' ' << 1 << endl;
    return 0;
}
