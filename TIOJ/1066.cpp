#include <iostream>
using namespace std;

int n, p[5], q[5], price;

int main() {
  cin.tie(0); ios_base::sync_with_stdio(false);
  cin >> n;
  while (n--) {
    cin >> price;
    for (int i = 0; i < 5; ++i) cin >> p[i];
    for (int i = 0; i < 5; ++i) cin >> q[i];
    
  }
}
