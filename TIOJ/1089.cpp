#include <iostream>
#include <utility>
using namespace std;

int n, k, r[501], c[501], x, y;
pair<int, int> r[501], c[501];

int main() {
  cin >> n >> k;
  for (int i = 0; i <= n; ++i) r[i].second = i;
  for (int i = 0; i < k; ++i) {
    cin >> y >> x;
    
  }
}
