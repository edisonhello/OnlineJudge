#include <bits/stdc++.h>
using namespace std;

const int maxn = 10 + 5;
int N, M, C[maxn][maxn];
vector<int> G[maxn];

int main() {
  ios_base::sync_with_stdio(false); cin.tie(0);
  cin >> M >> N;
  
  return 0;
}
