#include <bits/stdc++.h>
using namespace std;

vector<string> v;

int main() {
  ios_base::sync_with_stdio(false); cin.tie(0);
  for (int i = 0; i < 9; ++i) {
    string s; cin >> s;
    v.push_back(s);
  }
  
  return 0;
}
