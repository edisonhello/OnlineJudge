#include <iostream>
#include <vector>
#include <tuple>
using namespace std;
typedef tuple<int, int, int> Edge;

vector<Edge> G[40010];

int n, m, a, b, d, n1, n2, c1, p1, c2, p2;
inline int rit();

int main() {
  n = rit(), m = rit(), a = rit(), b = rit(), d = rit();
  
}
