#include <bits/stdc++.h>
using namespace std;

const int maxn = 5e4 + 10, maxq = 1e4 + 10;

int main() {
    int t; cin >> t; while (t--) {
        int n, q; cin >> n >> q;
        assert(n < maxn && q < maxq);
        int a, b, c, d, e;
        for (int i = 0; i < n; ++i) cin >> a;
        for (int i = 0; i < q; ++i) {
            cin >> a;
            if (a == 1) cin >> b >> c >> d >> e;
            if (a == 2) cin >> a
        }
    }
}
