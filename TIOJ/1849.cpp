#include <iostream>
#include <string>
#include <map>
using namespace std;

int main() {
  ios::sync_with_stdio(false);
  cin.tie(0);
  int n;
  cin >> n;
  string s;
  string method, name, message;
  for (int i = 0; i < n; ++i) {
    getline(cin, s);

    if (method == "\'NEW\'") {

    } else if (method == "\'Get\'") {

    } else if (method == "\'Delete\'") {

    } else if (method == "\'Post\'") {

    }
  }
}
