#include <bits/stdc++.h>
using namespace std;

const int maxn = 2000 + 5;
int T, n, a[maxn];

int main() {
  ios_base::sync_with_stdio(false); cin.tie(nullptr);
  cin >> T;
  while (T--) {
    cin >> n;
    for (int i = 0; i < n; ++i) cin >> a[i];
    
  }
  return 0;
}
