#include <bits/stdc++.h>
using namespace std;

const int maxn = 100 + 5;
int x, dp[maxn], a[maxn], n;

int main() {
  ios_base::sync_with_stdio(false); cin.tie(nullptr);
  while (cin >> a[1]) {
    for (int i = 2; ; ++i) {
      cin >> a[i];
      if (a[i] == -999999) { n = i - 1; break; }
    }
    for (int i = 1; )
  }
  return 0;
}
