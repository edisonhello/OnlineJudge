#include <iostream>
#include <stack>
using namespace std;

int h[100000];
int main() {
  stack<int> s1, s2;
  int n;
  cin >> n;
  for (int i = 0; i < n; ++i) cin >> h[i];
  for (int i = 0; i < n; ++i) {
    for (int j = i; j < n; ++j) {
      
    }
  }
}
